[tox]
envlist = linting

[testenv]
deps=
    pytest
  
commands=
    pytest {posargs}


[testenv:linting]
skip_install = True
basepython = python3.6
deps = pre-commit
commands = pre-commit run --all-files --show-diff-on-failure

[flake8]
max-line-length = 120